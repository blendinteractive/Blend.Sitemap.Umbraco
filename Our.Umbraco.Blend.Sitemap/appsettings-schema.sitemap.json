{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "SitemapAppSettings",
  "type": "object",
  "properties": {
    "Sitemap": {
      "type": "object",
      "description": "Definitions for the Blend.Sitemap",
      "properties": {
        "ExcludeBoolFieldAlias": {
          "type": "string",
          "description": "Alias of a true/false field for spefied content to not be in sitemap.",
          "default": null
        },
        "CacheMinutes": {
          "type": "integer",
          "description": "Number of minutes to cache the sitemap for. This will help with performance.",
          "default": 15
        },
        "IncludePageImages": {
          "type": "boolean",
          "description": "Add the page images to the sitemap.",
          "default": false
        },
        "IncludePageDocuments": {
          "type": "boolean",
          "description": "Add the page documents to the sitemap. Will be the same as the parent document.",
          "default": false
        },
        "DocumentTypes": {
          "type": "array",
          "description": "List of document types that are in the sitemap.",
          "items": {
            "$ref": "#/definitions/SitemapDocumentType"
          }
        }
      }
    }
  },
  "definitions": {
    "SitemapDocumentType": {
      "type": "object",
      "description": "Document type sitemap definition.",
      "properties": {
        "Aliases": {
          "type": "array",
          "description": "Array of document type aliases",
          "items": {
            "type": "string"
          },
          "default": null
        },
        "ChangeFrequency": {
          "type": "string",
          "description": "Change Frequency for these docuemnt types. Options: always, hourly, daily, weekly, monthly, yearly, never",
          "default": "weekly"
        },
        "Priority": {
          "type": "string",
          "description": "Priority for the these document types. 0.1 - 1.0",
          "default": "0.5"
        }
      }
    }
  }
}